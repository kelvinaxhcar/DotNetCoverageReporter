name: .NET CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Setup .NET SDK
        uses: actions/setup-dotnet@v1
        with:
          dotnet-version: '3.x'

      - name: Install compatible ReportGenerator tool
        run: dotnet tool install -g dotnet-reportgenerator --version 4.8.8
        # Replace '4.8.8' with a version that is compatible with .NET Core 3.1

      - name: Build project
        run: dotnet build --configuration Release ProjetoDeTeste/ProjetoDeTeste.csproj
        # Adjusted the build command to specify the .csproj file path

      - name: Run tests with coverage
        run: dotnet test --configuration Release --collect:"XPlat Code Coverage" ProjetoDeTeste/ProjetoDeTeste.csproj
        # Adjusted the test command to specify the .csproj file path

      - name: Find latest coverage file
        run: |
          testResultsDir=$(System.DefaultWorkingDirectory)/ProjetoDeTeste/ProjetoDeTeste/TestResults
          coverageFiles=$(find $testResultsDir -name 'coverage.cobertura.xml')
          if [ -z "$coverageFiles" ]; then
            echo 'Coverage file not found.'
            exit 1
          fi
          reportInput=$(ls -t $coverageFiles | head -n1)

      - name: Generate HTML report
        run: |
          reportOutput=$(System.DefaultWorkingDirectory)/ProjetoDeTeste/ProjetoDeTeste/Reports
          dotnet reportgenerator "-reports:$reportInput" "-targetdir:$reportOutput" "-reporttypes:HTML"

      - name: Move directories
        run: |
          mv $(System.DefaultWorkingDirectory)/ProjetoDeTeste/ProjetoDeTeste/TestResults $(System.DefaultWorkingDirectory)/destination
          mv $(System.DefaultWorkingDirectory)/ProjetoDeTeste/ProjetoDeTeste/Reports $(System.DefaultWorkingDirectory)/destination

      - name: Uninstall ReportGenerator tool
        run: dotnet tool uninstall -g dotnet-reportgenerator

      - name: Open report in browser
        run: |
          indexFile=$(System.DefaultWorkingDirectory)/destination/Reports/index.html
          if [ -f "$indexFile" ]; then
            echo 'Opening report in browser...'
            xdg-open file://$indexFile
          else
            echo "File $indexFile not found."
          fi
